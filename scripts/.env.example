# Step 3 (FAQ extraction pipeline) 用のシークレット
# このファイルをコピー: cp scripts/.env.example scripts/.env

# OpenAI API key — text-embedding-3-small 用 (約 $0.006/1万メッセージ)
OPENAI_API_KEY=sk-proj-...

# Anthropic API key — クラスタ命名用 Claude Sonnet 4.6 (約 $0.5/1万メッセージ)
ANTHROPIC_API_KEY=sk-ant-api03-...

# (Optional) 結果書き戻し用 — Step3 後半で使う
# WORKER_URL=http://localhost:5173
# LH_API_KEY=wara1534
